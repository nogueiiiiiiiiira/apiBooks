generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Book {
  id Int @id @default(autoincrement())
  nome String
  descricao String
  autor String
  valor String
  categoria String
}

model Librarian {
  id Int @id @default(autoincrement())
  nome String
  cpf String
  email String
  telefone String
  dataNasc DateTime
}
